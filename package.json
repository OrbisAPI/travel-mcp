{
  "name": "@orbisapi/travel-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "500+ travel APIs for AI agents — flights, hotels, destinations. Pay per call with USDC via x402.",
  "scripts": {
    "start": "node example.mjs"
  },
  "dependencies": {
    "viem": "^2.0.0",
    "x402-fetch": "^0.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
